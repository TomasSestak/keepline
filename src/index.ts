/**
 * keepline — the framework-agnostic core.
 *
 * React bindings live in `keepline/react`, so importing this entry from a
 * non-React app (or a Node service) pulls in no React at all.
 */
export * from './core/index';
